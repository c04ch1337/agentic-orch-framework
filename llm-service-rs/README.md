# LLM Service

## Overview
Interface to Large Language Model providers (OpenAI, Anthropic, Local). Handles text generation and embedding requests for the entire system.

## Features
- Unified API for multiple LLM backends
- Text generation and streaming
- Text embedding for vector search

## Usage
This service provides gRPC endpoints for text generation and embedding.
